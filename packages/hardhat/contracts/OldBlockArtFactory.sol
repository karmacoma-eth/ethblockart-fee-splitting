// SPDX-License-Identifier: AGPL-3.0-or-later

pragma solidity ^0.8.0;

contract OldBlockArtFactory {
    function getPsfb(uint256 blockNumber) external pure returns (uint256) {
        return 0;
    }

    function isStyleListed(uint256 styleId) external pure returns (bool) {
        return true;
    }
}